{
  "name": "inline-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite serve --mode dev",
    "build": "vite build --mode dev",
    "start": "vite preview --mode dev",
    "build:ly": "vite build --mode prod-layered",
    "build:uf": "vite build --mode prod-uf",
    "build:sh": "vite build --mode prod-shop",
    "build:all": "npm run build:ly && npm run build:sh && npm run build:uf"
  },
  "devDependencies": {
    "@rollup/pluginutils": "^5.1.4",
    "postcss": "^8.5.2",
    "prettier": "^3.5.3",
    "sass-embedded": "^1.83.4",
    "sharp": "^0.33.5",
    "terser": "^5.39.0",
    "vite": "^6.0.5",
    "vite-plugin-handlebars": "^2.0.0",
    "vite-plugin-html": "^3.2.2",
    "vite-plugin-singlefile": "^2.1.0"
  }
}
